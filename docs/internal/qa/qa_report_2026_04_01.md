# Meg QA Report - 2026-04-01

**Overall Status: GREEN**

All 261 tests pass (34 new). No critical bugs found. Three previously reported findings (MEG-031, MEG-032, MEG-033) are now RESOLVED. Two MEDIUM advisories from prior reports remain open but are acceptable for MVP. One new LOW advisory (SQL Optimizer SKILL.md license mismatch).

---

## Summary

This QA run covered 6 commits from the last 24 hours (db60f19, 01dc048, c150b84, 535a537, 3c5d1a7, d18debf). Major themes: public repo hygiene (internal docs moved out, .gitignore safety nets added, scrub/verify scripts), BSL 1.1 license rollout, LoreDocs README creation, and LoreConvo session limit enforcement.

Key changes reviewed:

1. **Public repo security hardening** (01dc048, c150b84, 535a537): Internal business documents (revenue projections, PRDs, PUBLISHING.md, marketplace listings) moved from product directories to docs/internal/. Product .gitignore files now block CLAUDE.md, *.xlsx, *.env. New scripts: scrub_public_repos.sh (git history rewrite), verify_public_repo_clean.sh (pre-push check), cleanup_internal_docs.sh (file mover).

2. **BSL 1.1 license enforcement** (d18debf, 0502423): LoreConvo and LoreDocs now have BSL 1.1 LICENSE files. LoreConvo database.py enforces 50-session free-tier limit with SessionLimitReachedError. Config.is_pro checks LORECONVO_PRO env var.

3. **LoreDocs README** (db60f19): New README.md with full tool table (34 tools), quick start, usage guide, license section. Well-structured and complete.

4. **LoreConvo auto_save.py FTS fix** (db60f19): FTS schema drift (MEG-032) resolved -- both database.py and auto_save.py now use identical FTS5 schema. NOT NULL constraint (MEG-033) also fixed.

5. **SQL Optimizer SKILL.md** (db60f19): Version corrected from 1.0.0 to 0.1.0 (MEG-031 resolved).

---

## Tests Run

| Suite | Files | Tests | Result |
|---|---|---|---|
| LoreConvo core | test_auto_load, test_vault_suggest | 41 | PASS |
| LoreConvo auto_save | test_auto_save | 17 | PASS |
| LoreConvo database | test_database_new | 21 | PASS |
| LoreConvo CLI | test_cli | 14 | PASS |
| LoreConvo NULL-id migration | test_null_id_migration | 10 | PASS |
| LoreConvo session limit | test_session_limit | 12 | PASS |
| **LoreConvo gitignore safety (NEW)** | **test_gitignore_safety** | **6** | **PASS** |
| LoreDocs core | test_storage, test_tiers | 42 | PASS |
| LoreDocs phase2 | test_phase2 | 6 | PASS |
| **LoreDocs README tools (NEW)** | **test_readme_tools** | **7** | **PASS** |
| **LoreDocs gitignore safety (NEW)** | **test_loredocs_gitignore** | **6** | **PASS** |
| LoreDocs MCP tools | test_mcp_tools | skipped | Missing `mcp` module in system Python |
| LoreDocs tier integration | test_tier_integration | skipped | Missing `mcp` module in system Python |
| SQL Analyzer | test_analyzer | 34 | PASS |
| SQL Credits | test_credits | 13 | PASS |
| SQL Credits edge cases | test_credits_concurrency | 8 | PASS |
| SQL Optimizer | test_optimizer | 17 | PASS |
| SQL Security | test_security_hardening | 29 pass, 6 skip | PASS (skips need FastAPI) |
| **Total** | | **261 (+ 8 skipped)** | **261 pass, 0 fail** |

---

## New Tests Written

### 1. `ron_skills/loreconvo/tests/test_gitignore_safety.py` (6 tests)

Verifies .gitignore blocks sensitive file types from public repos:

- **test_blocks_claude_md**: CLAUDE.md in .gitignore
- **test_blocks_xlsx_files**: *.xlsx in .gitignore
- **test_blocks_env_files**: *.env in .gitignore
- **test_blocks_database_files**: *.db in .gitignore
- **test_blocks_venv**: .venv/ or venv/ in .gitignore
- **test_no_sensitive_files_exist_unignored**: Belt-and-suspenders check that no sensitive files (revenue projections, PRDs, etc.) exist in the product directory

### 2. `ron_skills/loredocs/tests/test_readme_tools.py` (7 tests)

Validates LoreDocs README documentation matches server implementation:

- **test_tool_count_matches_34**: Verifies 34 @mcp.tool decorators in server.py
- **test_readme_claims_34_tools**: README text says "34 MCP tools"
- **test_readme_table_lists_all_tools**: Every server tool appears in README table and vice versa
- **test_readme_tool_count_equals_server**: Counts match between server and README
- **test_mentions_bsl**: README mentions BSL 1.1 license
- **test_mentions_free_tier_limit**: README mentions 3 vaults free tier
- **test_mentions_apache_conversion**: README mentions Apache 2.0 conversion

### 3. `ron_skills/loredocs/tests/test_loredocs_gitignore.py` (6 tests)

Same gitignore safety checks as LoreConvo, applied to LoreDocs product directory.

---

## Findings

### MEG-034: LOW - SQL Optimizer SKILL.md still says license: MIT-0

**Location**: ron_skills/sql_query_optimizer/skill/SKILL.md line 7

The SKILL.md metadata says `license: MIT-0` but LoreConvo and LoreDocs have switched to BSL 1.1. If the SQL Optimizer will also use BSL 1.1, the SKILL.md should be updated. If it intentionally uses MIT-0 (as a different product strategy), this is fine -- but it should be a documented decision.

### MEG-035: LOW - SQL Optimizer SKILL.md references railway.app URLs

**Location**: ron_skills/sql_query_optimizer/skill/SKILL.md lines 57, 92

The SKILL.md still references `https://sidehustle-production-6478.up.railway.app` as the premium API endpoint. If the API is not currently deployed on Railway, these URLs should be updated or marked as "coming soon" to avoid user confusion.

### Previously Noted (still open)

- **MEDIUM**: credits.py uses non-atomic JSON file read/write with no locking (from 2026-03-30 report). Acceptable for MVP.
- **MEDIUM**: optimizer.py uses sync Anthropic client inside async function (from 2026-03-30 report). Acceptable for MVP.

### Resolved Since Last Report

- **MEG-031**: RESOLVED. SQL Optimizer version mismatch fixed (SKILL.md now says 0.1.0, matching CLAUDE.md).
- **MEG-032**: RESOLVED. FTS schema drift between database.py and auto_save.py fixed -- both now use identical `fts5(title, summary, decisions, content=sessions, content_rowid=rowid)`.
- **MEG-033**: RESOLVED. auto_save.py now uses `id TEXT PRIMARY KEY NOT NULL`, matching database.py.

---

## Code Walkthrough Notes

### Public Repo Hygiene Scripts (GOOD)

Three well-structured bash scripts added:

- **scrub_public_repos.sh**: Uses git-filter-repo to remove sensitive files from GitHub history. Has proper confirmation prompt, error handling, and post-scrub instructions. Uses `set -euo pipefail` for safety.
- **verify_public_repo_clean.sh**: Pre-push check that scans for blocked patterns in tracked files. Returns exit code 1 on failure (usable as pre-push hook). Comprehensive pattern list.
- **cleanup_internal_docs.sh**: Would move files to docs/internal/. Not reviewed in detail as the moves were already done manually.

### LoreConvo Session Limit (GOOD)

The BSL 1.1 free-tier enforcement is clean:
- `SessionLimitReachedError` has a clear docstring explaining the legal basis
- `save_session()` checks count before INSERT -- prevents partial state
- `Config.is_pro` is a simple env var check with `.strip()` to handle whitespace
- Error message includes upgrade path (email, env var)
- Test coverage is thorough (12 tests covering edge cases)

### LoreDocs README (GOOD)

Well-structured README with:
- Clear quick start (uv-based)
- Both Code and Cowork setup instructions
- Complete MCP tool table organized by function (6 categories, 34 tools)
- Troubleshooting section addresses common issues
- License section reflects BSL 1.1 accurately

### .gitignore Files (GOOD)

Both product .gitignore files block the right patterns. The comment at the top ("Internal docs belong in docs/internal/") provides clear guidance. Blocking *.db and *.sqlite prevents accidental database commits.

---

## Doc Verification Summary

| Product | Version Match | Tool Count Match | License Match | Issues |
|---|---|---|---|---|
| LoreConvo | v0.3.0 consistent | 12 tools match | BSL 1.1 | None |
| LoreDocs | v0.1.0 consistent | 34 tools match | BSL 1.1 | None |
| SQL Query Optimizer | 0.1.0 consistent | N/A (skill, no MCP server count) | **MIT-0 (not BSL)** | MEG-034 |

---

## Security Spot Check

- No hardcoded secrets in source files (grep clean)
- No .env files tracked in git (git ls-files clean)
- No internal business docs in product directories (moved to docs/internal/)
- SQL injection: all queries use parameterized statements
- Path traversal: no "../" patterns in file handlers
- .gitignore safety nets in place for both public-facing products
- Pre-push verification script available (verify_public_repo_clean.sh)
- API keys stored as SHA256 hashes only (SQL Optimizer)

**Status: SECURE**

---

## Trend vs Previous Report (2026-03-31)

| Metric | 2026-03-31 | 2026-04-01 | Delta |
|---|---|---|---|
| Total tests | 227 | 261 | +34 |
| Passing | 227 | 261 | +34 |
| Failing | 0 | 0 | -- |
| Skipped | 8 | 8 | -- |
| New tests written | 18 | 19 | -- |
| Findings (new) | 1 MEDIUM, 2 LOW | 2 LOW | Improved |
| Findings resolved | -- | 3 (MEG-031/032/033) | -- |
| Overall status | YELLOW | GREEN | Improved |
