{
  "mcpServers": {
    "loredocs": {
      "command": "${CLAUDE_PLUGIN_ROOT}/.venv/bin/python3",
      "args": [
        "-m",
        "loredocs.server"
      ],
      "env": {
        "PYTHONPATH": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}
